/*
 * toolbox.h
 *
 *  Created on: May 23, 2017
 *      Author: colin
 */

#ifndef TOOLBOX_H_
#define TOOLBOX_H_

#define MIN(X, Y) (((X) < (Y)) ? (X) : (Y))
#define MAX(X, Y) (((X) > (Y)) ? (X) : (Y))

#endif /* TOOLBOX_H_ */
